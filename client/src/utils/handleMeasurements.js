//TODO

// convert all measurements to a default (e.g. "fluid oz", "metric")

//usage: handleMeasurements([arrayOfMeasurements])

export const handleMeasurements = (measurements) => {

  const newMeasurements = measurements.map(measurement => {
    
    return newMeasurement;
  })
}